dict<int, char> test = {1: 'a', 2: 'b', 3: 'c'};
int hello = test + test[1];

string getdict = hello_[1][2][3]['c'];

char minus = 4 - 5;
char minus2 = -test;

char brackets = -(21 * WoW) + (test/523);
float power = 12^2;
rat rattest = 21_21/31;

int nottest = !hello;
bool andtest = T && F;
bool ortest = !(F || T) || F;

bool gttest = (5 < 6) && T;
bool gtequal = (5 <= 2121) || F;

bool equals = T == T;
bool nequals = T != (5 < 6);

int hello = test :: 1;
bool inside = 5 in test;

int lentest = len([1, 2, 3]);

seq<int> wohoow = [1, 2, 3][1:2];
seq<bool> wohoow2 = test[:3];
seq<float> wohoow3 = test[4:];

float functest = hello(1, 2, 3, hello);
float functest2 = hello_();

int aliasaccess = test.test;
int aliasaccess = test[2].test;

