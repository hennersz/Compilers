int i = 0;
while(i < 10) do
    i = i + 1;
    string test = "Hello";
    seq<char> chars= ['H', 'e', 'l', 'l', 'o'];

od

seq<int> items = [1, 2, 3, 4, 5];

forall(item in items) do
    item = item + 1;

od
